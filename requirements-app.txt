# Slim dependency set for HOSTING the Streamlit app with the shipped
# baseline (TF-IDF + MLP) model. No torch/transformers needed — keeps the
# Streamlit Community Cloud build fast and within memory limits.
#
# To deploy: point Streamlit Cloud at app.py and set this file as the
# requirements file (or copy it over requirements.txt on a deploy branch).
streamlit>=1.34
plotly>=5.21
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
joblib>=1.4
